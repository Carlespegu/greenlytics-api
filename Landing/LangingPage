import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { motion } from "framer-motion";
import { Leaf, BarChart3, Cpu, Droplets } from "lucide-react";

export default function GreenlyticsLanding() {
  return (
    <div className="min-h-screen bg-white text-gray-900">
      {/* HERO */}
      <section className="px-6 py-20 max-w-7xl mx-auto grid md:grid-cols-2 gap-10 items-center">
        <div>
          <h1 className="text-4xl md:text-5xl font-bold leading-tight mb-6">
            Monitoritza i optimitza la salut de les teves plantes amb dades en temps real
          </h1>
          <p className="text-lg text-gray-600 mb-8">
            GreenLytics combina sensors IoT i analítica avançada per ajudar-te a prendre decisions intel·ligents i millorar el creixement de les plantes.
          </p>
          <div className="flex gap-4">
            <Button className="text-lg px-6 py-4">Demana una demo</Button>
            <Button variant="outline" className="text-lg px-6 py-4">Veure com funciona</Button>
          </div>
        </div>

        <motion.div
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.6 }}
          className="bg-gray-100 rounded-2xl p-6 shadow-lg"
        >
          <p className="text-center text-gray-500">[Screenshot dashboard aquí]</p>
        </motion.div>
      </section>

      {/* PROBLEM */}
      <section className="bg-gray-50 py-16 px-6">
        <div className="max-w-5xl mx-auto text-center">
          <h2 className="text-3xl font-bold mb-6">El problema</h2>
          <p className="text-lg text-gray-600">
            No saber quan regar, si una planta està en el lloc adequat o si rep les condicions correctes pot provocar pèrdues i decisions incorrectes.
          </p>
        </div>
      </section>

      {/* SOLUTION */}
      <section className="py-16 px-6 max-w-6xl mx-auto grid md:grid-cols-3 gap-6">
        {[{
          icon: <Cpu />, title: "Sensors intel·ligents", text: "Recull dades de temperatura, humitat i llum automàticament"
        },{
          icon: <BarChart3 />, title: "Analítica avançada", text: "Visualitza dades i tendències en temps real"
        },{
          icon: <Leaf />, title: "Recomanacions", text: "Pren decisions basades en dades per millorar el creixement"
        }].map((item, i) => (
          <Card key={i} className="rounded-2xl shadow-sm">
            <CardContent className="p-6 text-center">
              <div className="mb-4 flex justify-center">{item.icon}</div>
              <h3 className="font-semibold text-xl mb-2">{item.title}</h3>
              <p className="text-gray-600">{item.text}</p>
            </CardContent>
          </Card>
        ))}
      </section>

      {/* HOW IT WORKS */}
      <section className="bg-gray-50 py-16 px-6">
        <div className="max-w-6xl mx-auto">
          <h2 className="text-3xl font-bold text-center mb-10">Com funciona</h2>
          <div className="grid md:grid-cols-3 gap-6">
            {["Instal·la sensors", "Recull dades automàticament", "Analitza i optimitza"].map((step, i) => (
              <Card key={i}>
                <CardContent className="p-6 text-center">
                  <h3 className="font-semibold text-xl">{step}</h3>
                </CardContent>
              </Card>
            ))}
          </div>
        </div>
      </section>

      {/* BENEFITS */}
      <section className="py-16 px-6 max-w-6xl mx-auto grid md:grid-cols-3 gap-6">
        {["Redueix la mortalitat de plantes", "Optimitza el consum d’aigua", "Decisions basades en dades"].map((b, i) => (
          <Card key={i}>
            <CardContent className="p-6 text-center">
              <Droplets className="mx-auto mb-4" />
              <p className="font-medium">{b}</p>
            </CardContent>
          </Card>
        ))}
      </section>

      {/* CTA */}
      <section className="bg-green-600 text-white py-20 text-center px-6">
        <h2 className="text-3xl font-bold mb-6">Comença a optimitzar les teves plantes avui</h2>
        <Button className="text-lg px-8 py-4 bg-white text-green-600">
          Demana una demo
        </Button>
      </section>

      {/* FOOTER */}
      <footer className="py-10 text-center text-gray-500">
        © {new Date().getFullYear()} GreenLytics
      </footer>
    </div>
  );
}
